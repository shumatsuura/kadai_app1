<head>
  <title>つぶやく？</title>
</head>
<body>

  <header>
  みんなの匿名つぶやき掲示板
  </header>

  <h1>つぶやき一覧</h1>

  <p><%= notice %></p>

  <table>
  <% @tweets.each do |tweet| %>
    <tr>
      <td><%= tweet.content %></td>
      <td><%= link_to "編集", edit_tweet_path(tweet.id) %></td>
      <td><%= link_to "削除", tweet_path(tweet.id), method: :delete ,data: {confirm: '本当に削除していいですか？'}%></td>
    </tr>
  <% end %>
  </table>

  <div class="new_tweet">
  <%= link_to '新しくつぶやく',new_tweet_path %>
  </div>

  <footer>copyright (c)【2020】 【Shunsuke Matsuura】 all rights reserved.</footer>

  </body>
